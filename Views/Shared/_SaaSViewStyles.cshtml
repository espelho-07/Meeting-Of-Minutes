<style>
    .view-page { width: min(100%, 1320px); margin: 0; }
    .view-hero { display: grid; grid-template-columns: minmax(0, 1.2fr) minmax(280px, 0.7fr); gap: 16px; align-items: stretch; margin-bottom: 18px; }
    .view-hero-main { position: relative; overflow: hidden; border-radius: 24px; padding: 24px 26px; background: linear-gradient(135deg, #eef2ff 0%, #f7f4ff 48%, #ffffff 100%); border: 1px solid rgba(120, 102, 255, 0.14); box-shadow: 0 18px 44px rgba(54, 63, 121, 0.08); }
    .view-hero-main::before, .view-hero-main::after { content: ""; position: absolute; border-radius: 999px; pointer-events: none; }
    .view-hero-main::before { width: 210px; height: 210px; right: -54px; top: -94px; background: radial-gradient(circle, rgba(108, 99, 255, 0.18) 0%, rgba(108, 99, 255, 0) 70%); }
    .view-hero-main::after { width: 160px; height: 160px; left: -42px; bottom: -72px; background: radial-gradient(circle, rgba(139, 92, 246, 0.14) 0%, rgba(139, 92, 246, 0) 70%); }
    .view-kicker { display: inline-flex; align-items: center; gap: 8px; padding: 7px 11px; border-radius: 999px; background: rgba(255,255,255,.74); border: 1px solid rgba(114,97,255,.12); color: #5b59d9; font-size: .72rem; font-weight: 800; letter-spacing: .08em; text-transform: uppercase; margin-bottom: 14px; position: relative; z-index: 1; }
    .view-title { margin: 0 0 10px; color: #18233f; font-size: clamp(2rem, 3vw, 2.85rem); line-height: 1.02; font-weight: 900; letter-spacing: -.05em; max-width: 10ch; position: relative; z-index: 1; }
    .view-subtitle { margin: 0; max-width: 56ch; font-size: .98rem; line-height: 1.65; color: #60708d; position: relative; z-index: 1; }
    .view-note { display: grid; gap: 12px; align-content: start; background: rgba(255,255,255,.88); border: 1px solid #e8ebf8; border-radius: 22px; padding: 20px; box-shadow: 0 14px 34px rgba(44,52,92,.07); }
    .view-note-label { color: #8996ab; font-size: .72rem; font-weight: 800; text-transform: uppercase; letter-spacing: .08em; }
    .view-note-title { margin: 0; color: #1e2a48; font-size: 1.18rem; line-height: 1.22; font-weight: 800; letter-spacing: -.03em; }
    .view-note-list { display: grid; gap: 9px; margin: 0; padding: 0; list-style: none; }
    .view-note-list li { display: flex; gap: 10px; align-items: flex-start; color: #66748f; font-size: .92rem; line-height: 1.55; }
    .view-note-list i { color: #6658f2; margin-top: 3px; }
    .view-grid { display: grid; grid-template-columns: 1.1fr 0.9fr; gap: 16px; margin-bottom: 18px; }
    .view-card { background: rgba(255,255,255,.96); border: 1px solid #e9edf7; border-radius: 24px; box-shadow: 0 18px 44px rgba(32,41,76,.07); overflow: hidden; }
    .view-card-header { display: flex; align-items: center; justify-content: space-between; gap: 14px; padding: 18px 22px 14px; border-bottom: 1px solid #edf1f8; }
    .view-card-heading { margin: 0; color: #1b2747; font-size: 1.06rem; font-weight: 800; letter-spacing: -.02em; }
    .view-card-meta { margin: 4px 0 0; color: #7b88a0; font-size: .87rem; }
    .view-card-body { padding: 22px; }
    .view-detail-grid { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 14px; }
    .view-detail-item { border: 1px solid #ebf0f7; background: #fbfcff; border-radius: 16px; padding: 14px 16px; min-height: 100%; }
    .view-detail-item.wide { grid-column: 1 / -1; }
    .view-detail-label { display: block; color: #8a97ac; text-transform: uppercase; letter-spacing: .06em; font-size: .72rem; font-weight: 800; margin-bottom: 7px; }
    .view-detail-value { color: #202b46; font-size: 1rem; font-weight: 700; line-height: 1.6; word-break: break-word; }
    .view-detail-value.muted { color: #6f7d95; font-weight: 600; }
    .view-badge { display: inline-flex; align-items: center; gap: 6px; padding: 7px 11px; border-radius: 999px; background: #f4f6ff; border: 1px solid #e4e8ff; color: #5a55db; font-size: .78rem; font-weight: 800; }
    .view-stat-stack { display: grid; gap: 12px; }
    .view-stat-tile { border: 1px solid #ebf0f7; background: linear-gradient(180deg, #ffffff 0%, #fbfcff 100%); border-radius: 18px; padding: 16px; }
    .view-stat-number { font-size: 1.8rem; line-height: 1; font-weight: 900; letter-spacing: -.04em; color: #1f2a49; margin-bottom: 6px; }
    .view-stat-caption { color: #6f7d95; font-size: .88rem; font-weight: 700; }
    .view-actions { display: flex; gap: 10px; flex-wrap: wrap; }
    .view-btn { min-height: 42px; padding: 0 16px; border-radius: 12px; font-weight: 800; font-size: .9rem; }
    .view-btn-primary { color: #fff; border: 0; background: linear-gradient(135deg, #635bff 0%, #7a4dce 100%); box-shadow: 0 12px 24px rgba(99, 91, 255, 0.18); }
    .view-btn-light { border: 1px solid #dbe3ef; background: #fff; color: #273350; box-shadow: 0 6px 14px rgba(38, 46, 78, 0.05); }
    .view-btn-danger { border: 1px solid #ffd7d9; background: #fff5f5; color: #d6494f; }
    .view-list { display: grid; gap: 10px; }
    .view-list-row { border: 1px solid #ebf0f7; background: linear-gradient(180deg, #ffffff 0%, #fbfcff 100%); border-radius: 16px; padding: 14px 16px; }
    .view-list-title { color: #1f2a49; font-weight: 800; margin-bottom: 4px; }
    .view-list-meta { color: #7f8aa0; font-size: .86rem; }
    @@media (max-width: 991px) { .view-page { width: 100%; } .view-hero, .view-grid { grid-template-columns: 1fr; } .view-hero-main, .view-note, .view-card-body, .view-card-header { padding: 20px; } .view-title { max-width: none; font-size: 2.2rem; } }
    @@media (max-width: 767px) { .view-detail-grid { grid-template-columns: 1fr; } .view-actions { flex-direction: column; } .view-btn { width: 100%; } }
</style>
